export interface ButtonProps {
  disabled?: boolean;
  secondary?: boolean;
  className?: string;
  type?: 'button' | 'submit' | 'reset';
}

export interface StyledButtonProps {
  disabled?: boolean;
  className?: string;
  type?: 'button' | 'submit' | 'reset';
}
